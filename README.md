# Analog Tears UI

Analog Tears UI is a user interface for the Analog Tears project, designed to provide an intuitive and user-friendly
experience for users interacting with the Analog Tears test fire System.


## Setup
Make sure to run `pip install -r requirements.txt` (or `uv pip install -r requirements.txt` if you have uv) to install
all necessary dependencies before running the project.